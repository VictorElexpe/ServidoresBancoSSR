a(href='/logout') Cerrar sesión!

h1= "Hola, " + user.firstName + " " + user.lastName + "!"
p= user.email
p= "Código de empleado: " + user.employeeCode
h3= "Dar de alta nuevo cliente en el banco"
form(method='POST', action='/newClient')
  .form-group
  label(for='firstName') Nombre:
  input#pw.form-control(type='firstName', name='firstName')
  br
  .form-group
  label(for='lastName') Apellidos:
  input#pw.form-control(type='lastName', name='lastName')
  br
  .form-group
  label(for='email') Email:
  input#email.form-control(type='email', placeholder='nombre@email.com', name='email')
  br
  .form-group
  label(for='password') Contrase&ntilde;a:
  input#pw.form-control(type='password', name='password')
  br
  button.btn.btn-primary(type='submit') Dar de alta nuevo cliente